# Leave empty to use proxy (Vite dev or nginx prod).
# For Render production: set to the backend URL, e.g. https://games-backend.onrender.com
# For local dev: keep empty so Vite proxy forwards /api to localhost:8080
VITE_API_BASE_URL=
